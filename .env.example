# Application Configuration
PORT=3000
NODE_ENV=production

# Database Configuration
MONGO_URI=mongodb://mongodb:27017/test

# Session Configuration
SESSION_SECRET=your_secure_session_secret_here_32_characters_minimum

# Email Configuration (Gmail example)
# Required for email OTP verification and notifications
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit .env file to version control
# 4. For Gmail, use App Passwords instead of your regular password
#    - Go to Google Account settings > Security > App passwords
#    - Generate a new app password for this application
# 5. Ensure IMAP is enabled in your Gmail settings for email sending
